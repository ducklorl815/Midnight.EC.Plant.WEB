# 我的植栽

個人植栽照護與日記產品的領域用語。本文件只定義名詞，不含實作細節。

## Language

**我的植栽**：
產品顯示名稱；導覽品牌標與「我的植栽」選單項指向植栽清單；頁面標題亦可出現此品牌稱呼。
_Avoid_: Midnight.EC.Plant.WEB、Plant App、把產品名當拼圖首頁的唯一入口稱呼

**植栽清單**：
全盆照護總覽頁：健康度、澆水倒數、各盆用肥料倒數，並可快速澆水／快速施肥寫入照護紀錄。導覽「我的植栽」進入此頁；「首頁」仍為模組拼圖。
_Avoid_: SearchList（對使用者溝通）、待辦面板、取代通知提醒

**盆用肥料**：
該盆自訂的施肥品項（名稱如 A肥／B肥、週期天數）；植栽清單分別倒數與快速施肥。與物種「施肥配方」通識分開。
_Avoid_: 全站共用肥料庫（第一版）、與施肥配方模組長文混稱

**植栽**：
使用者擁有並持續照顧的一盆（或一株）植物實體，有暱稱與可選物種。
_Avoid_: 植物（單獨出現、易與物種混淆）、Plant entity

**暱稱**：
植栽在畫面上的主名稱，由使用者命名，須可辨識、不可與其他盆重複。
_Avoid_: 名稱、DisplayName（對使用者溝通時）

**物種**：
該植栽對應的植物種類（中文名／學名等），可後補。
_Avoid_: 種類、品種（除非日後真的建模品種）

**滾動態殼層**：
首頁（與拼圖預覽）頂部導覽：**一進頁即 fixed 貼頂並隨滾軸跟著**；在 Banner 區上為透明疊圖、淺色字；滾過 Banner 底緣後改白底深色字（仍 fixed）。Header 背景與 Banner 主圖／底色全寬穿過殼層左右溝槽；標題／副標／CTA 等文案仍對齊殼層寬度。其餘模組仍在殼層內。
_Avoid_: 把 Banner 圖複製成 Header 的 background-image、全站無 Banner 頁也強制透明頂欄、未過 Banner 前用 absolute 讓頂欄滾走、把 Banner 文案一併拉滿視窗寬、內容模組一併拉滿視窗寬

**模組間留白**：
內容模組之間、以及 Banner 與下一模組之間，採明顯大間距（約 4–6rem 級）；Header 與 Banner 疊合、其間不加空白。拼圖預覽同一套（可視情況略縮）。殼層用 `gap` 統一間距；**Banner 另加一份同值 `margin-bottom`**（與 gap 疊加，使 Banner 下更疏、抵消弧切視覺），弧切本身保留。細節牆等專用樣式不得清掉殼層間距機制。最後一個模組與頁腳之間同樣有 gap。通知提醒無事不渲染，不另占位。
_Avoid_: 模組緊貼無呼吸感、在 Header–Banner 之間塞空白、`.pc-detail-wall { margin: 0 }` 這類破壞間距機制、無事通知提醒仍占空段

**Banner 模組**：
模組拼圖中釘在殼層下的首屏主視覺（以照片為主）；順序固定不可拖到其他模組之前，**必須開啟不可關閉**。底緣大弧切邊在**右下角**（非左下）。拼圖編輯以換圖為主：可**上傳到站點／拼圖專用媒體**，或**從自己所有植栽的全部照片（依盆分組）選圖**（存 plantId＋imageId，顯示時再解析）；外連網址屬進階或次要。引用圖若盆／照片已刪，回退占位並在編輯器提示「原圖已不在」。標題／副標／CTA 屬進階。編輯入口在設定→首頁拼圖。
_Avoid_: 可自由拖到拼圖任意位置、把 Banner 當普通可重排模組、允許關掉 Banner、編輯面以文案為主、左下大弧（已改右下）、把上傳檔硬塞進某一盆相簿、只存植栽圖 URL 卻宣稱與盆脫鉤（選盆圖應走 id 引用）、禁止刪除被 Banner 引用的照片（耦合過重）

**殼層導覽**：
首頁滾動態殼層上的品牌與主選單（首頁／我的植栽／新增／設定）。
_Avoid_: 與內頁實心 navbar 混稱、Layout 預設頂欄（首頁已改走殼層）

**殼層頁腳**：
首頁底部簡短版權／品牌列；白底＋細線分隔，輕量美化，不做成多欄行銷頁腳。
_Avoid_: 參考站完整 Footer 多欄、米色／奶油漸層頁腳、與內頁 footer 強行做成同一套複雜結構

**預設白底**：
產品預設紙色為純白（非米色、非奶油紙）；全站去掉米色裝飾底。內頁可保留極淺綠灰當頁面溝槽，不與米色混用。拼圖內容模組段底預設純白（CSS 覆蓋各 theme 名）；無圖占位為淺灰／近白，不用彩色漸層。資料可保留 theme 欄位，第一版不當成上色。Banner 上透明頂欄用純白字，並靠加強頂緣深色遮罩避免白疊白。
_Avoid_: 米色紙／奶油紙當預設底、Banner CTA 用米色塊、頂欄近白字卻無頂緣遮罩、把極淺綠灰誤改成米色、用 moss/clay 段底當預設視覺

**植栽細節牆**：
首頁模組拼圖中的固定左圖右文輪播；圖文互相置中，圖欄填滿模組左半並以 cover 延展；導覽為圖下方圓點。右文為暱稱（可點進詳情）、學名、短介紹與物種照護知識細項，不含實際環境圖示。
_Avoid_: 左右交錯圖文、側邊箭頭導覽、把實際環境三圖示當主內容、與照片牆混稱

**效果圖**：
由該盆封面經 AI 生成、只給植栽細節牆左圖用的裝飾圖；不取代相簿原檔。
_Avoid_: overlay、合成成品、把 latest 生成當作顯示欄位

**置換**：
把某張效果圖指定為該盆在植栽細節牆的左圖。
_Avoid_: 覆蓋封面、寫入相簿原檔、有生成就自動進首頁

**還原原圖**：
取消置換，植栽細節牆左圖回到封面。
_Avoid_: 刪除效果圖資產、還原取景

**通知提醒**：
首頁模組拼圖中的催辦區；只列已逾期的澆水／施肥，點列進詳情並可一鍵完成；無事則整塊不出現。不含 AI 異常／分析等觀察類。
_Avoid_: 今日行動（舊首頁用語）、待辦面板、任務列表、①②③、把健康度或 AI 異常塞進此區

**智慧提醒**：
系統依週期與分析為單盆產生的提醒實體（澆水、施肥、AI 分析、異常等）；詳情頁可略過。首頁通知提醒只取其中澆水／施肥逾期。
_Avoid_: Reminder（對使用者溝通時）、通知（單獨出現易與系統推播混淆）

**AI 異常預警**：
來自 AI 分析的觀察類提醒（如葉片需密切觀察）；屬日常觀察，留在詳情智慧提醒，不進通知提醒。
_Avoid_: 當成首頁可一鍵完成的催辦、與健康度徽章混稱

**今日行動**：
舊首頁任務區用語；模組拼圖首頁改由通知提醒承接。
_Avoid_: 當作現行首頁主催辦名稱

**待補**：
缺上次澆水日起點，或資料／環境未完成等需補齊的項目；屬次要催辦，不進通知提醒列表。
_Avoid_: 催補上次澆水日、資料未完成（分開稱呼）、Incomplete

**照片牆**：
首頁滿版展陳區，以封面為主；版面可由使用者編排（順序、跨欄／跨列、縫寬、格內取景），用來瀏覽植栽而非處理任務。不顯示「照片牆」標題。暱稱桌機滑過才淡入；手機不常駐。
_Avoid_: 植物牆、Plant grid、卡片牆、自動雜誌網格（僅無存檔時的種子）、牆上浮層導覽鈕

**牆版面**：
照片牆的持久化排版（欄數、預設縫寬、逐條縫覆寫、各格順序／span／取景）；整份存 DB。
_Avoid_: Layout JSON（對使用者溝通時）、CSS grid 設定

**編輯牆**：
設定內的「照片牆版面」獨立頁；可拖移、改 span、調縫（含點單條縫）、格內縮放位移並儲存。首頁不進入編輯模式。
_Avoid_: 首頁當場編輯、編輯模式（指首頁切換）

**縫**：
格子之間的間距。可有整牆預設直縫／橫縫，亦可逐條覆寫（`colGaps`／`rowGaps`）。
_Avoid_: 只能整牆一個 gap（已過時）、margin

**取景**：編輯牆時對單格封面縮放與位移；滾輪自由 scale（&gt;0，無上限）；放大可裁切、縮小留白；比例不合預設靠左下；不改原圖。
_Avoid_: 裁切存檔、固定 1～3 倍縮放

**跨列**：
單格往下佔 2 或 3 列高度；只加高該格，同列其他格不跟著變高；被跨過的縫僅在該格範圍內被蓋住。
_Avoid_: 整列拉高、自由像素高度

**封面**：
代表該植栽的主照片，用於照片牆與詳情辨識。
_Avoid_: 縮圖、頭圖（可接受但非首選）

**澆水逾期**：
距上次澆水已超過該盆澆水週期（已過天數＝今天−上次澆水−週期，且＞0），應出現在通知提醒的狀態。
_Avoid_: 過期、欠澆

**強調色**：
介面上唯一用來標示「該做的事」與主 CTA 的色彩角色（與品牌綠分離）。
_Avoid_: primary（易與品牌主色混淆）、accent（過泛）

**照護知識**：
物種級栽培知識，由「快速照護資訊」與「照護知識模組」組成；不是一篇長文。不含單盆的環境適配建議與使用者週期。
_Avoid_: PlantKnowledge（對使用者溝通時）、知識與照護說明（易與環境適配建議混稱）、養護重點總覽（已廢止）、把 ChatGPT 長文當唯一格式

**快速照護資訊**：
物種級結構化摘要（如日照枚舉、澆水策略、溫度區間、生長季）；供提醒、細節牆與詳情頂列。詳細解釋只存在對應照護知識模組。
_Avoid_: 與模組長文各寫一遍、建議日照與光照散文並陳、把「我的週期」寫進此層

**照護知識模組**：
單一主題的獨立知識單元（如光照、澆水），有穩定 id、內容與可選結構化欄；同一事實只歸一個主模組。
_Avoid_: 自由標題、為湊齊而硬產空模組、在總覽重抄各模組

**模組登錄**：
系統認定的模組目錄（id、標題、分類、預設順序）；決定「可以有哪些模組」。
_Avoid_: 讓 AI 自創 id、用中文標題當 id

**指南區塊順序**：
全站一份的模組顯示先後與開關（enabled）；與模組內容分離，於設定「照護指南版面」編排。無內容或未啟用則收合。第一版不含使用者自訂新增模組。
_Avoid_: 每盆／每物種各自順序（第一版）、把順序寫進物種 JSON、寫死唯一順序、第一版就做自訂模組 id

**施肥配方**：
物種級可執行水肥的結構化欄位（肥種、NPK、稀釋、頻率），由固定 UI 模板呈現；AI 只填欄位不寫配方長文。
_Avoid_: 與「施肥」模組長文重複描述、把 NPK 新手教學當物種專屬內容

**NPK 新手說明**：
通用知識（氮磷鉀意義、如何稀釋），不綁單一物種；依附施肥配方 UI 收合顯示，不由 AI 每盆重產。
_Avoid_: beginner-fertilizer 當物種模組內容、每物種重新生成一遍

**外部同步**：
舊路徑：從 Trefle／iNaturalist／GBIF 拉物種與照護資料。產品主路徑已改為 OpenAI；此詞僅描述歷史行為或遷移中殘留。
_Avoid_: 當作現行補知識手段、同步外部來源（對使用者溝通）、RefreshFromExternal

**AI 補足**：
用 OpenAI 產出或更新物種的快速照護資訊與照護知識模組（可覆蓋既有薄資料）；與「AI 分析」不同。
_Avoid_: AI 分析（另一條路徑）、只填空永不覆蓋（已過時）、同時強制短欄散文＋模組長文雙寫（已過時）

**中文找種**：
以使用者輸入的中文名（信任中文名）請 OpenAI 解析出候選學名與短辨識，供選取後換綁；確認後再 AI 補足寫滿照護知識。
_Avoid_: 手填學名再查（主路徑）、外部 API 中文搜尋、一次打滿所有候選的完整指南（費 token）

**照片找種**：
進階路徑：臨時上傳一張，或從該盆既有照片／封面選一張，請 OpenAI 協助辨識候選物種。
_Avoid_: 當成 AI 分析（診斷健康）、取代中文找種的主路徑、辨識照強制寫入日記

**重新產生照護知識**：
詳情頁主動作：以目前綁定物種請 OpenAI 強制重寫快速照護資訊與照護知識模組（可再觸發環境適配）。
_Avoid_: 同步外部 API、外部補圖／核對（已不做）**實際環境**：
該植栽目前放置條件（位置類型、日照、遮雨、介質、水盤狀態、縣市等），屬單盆事實，不是物種通識。
_Avoid_: 環境設定（過泛）、Profile（對使用者溝通時）

**水盤狀態**：
該植栽盆底水盤的實際使用情況：無水盤、有水盤但不積水、或有水盤且會蓄水。屬實際環境的一環；水盤主用途是接水，不是把蓄水期拉長。
_Avoid_: 是否需要附水盤（易與「建議」混淆）、HasSaucer（對使用者溝通時）

**記一筆**：
詳情頁一次記下某日的澆水／施肥／備註與可選照片；取代分開的「今日紀錄」與手動「照護紀錄」新增。
_Avoid_: 今日紀錄、照護紀錄表單（對使用者溝通時）

**位置備註**：
植栽擺放位置的自由文字說明（例如客廳窗台），僅在編輯頁選填；不進新增流程，不參與環境落差與環境適配 AI。
_Avoid_: 與「實際位置類型」混稱、Location（對使用者溝通時）、新增時再蒐集位置備註

**新增植栽流程**：
先填基本資料與實際環境六項，再確認物種後建檔；確認時強制同步物種知識並在環境齊全時產出環境適配建議。
_Avoid_: 先建檔再補環境才打適配 AI（舊流程）、因物種知識已存在而跳過同步

**重選物種**：
已建檔植栽事後更換綁定物種；主路徑為中文找種，進階可手調學名或照片找種；確認後換綁、以 AI 補足重寫照護知識並重算環境適配。
_Avoid_: 僅改學名字串不換綁、編輯頁「物種不可修改」、以手填學名為唯一入口

**信任中文名**：
僅來自使用者輸入或資料庫的中文名稱；AI 不得自行翻譯或改用其他俗名。
_Avoid_: AI 俗名、自動翻譯種名

**環境適配建議**：
依實際環境對照快速照護資訊／模組需求，由 AI 產出的單盆風險與調整建議；只做差異分析，不重抄物種模組長文。詳情以「我的環境」個人模組呈現，資料存於單盆 Profile。
_Avoid_: 知識與照護說明（物種通識）、ExternalCareGuide、在適配建議裡完整重述光照／澆水百科

**AI 分析**：
針對該盆／該張照片的診斷結果：對照實際環境與照護知識，產出已觀察、可能原因、可執行建議（含水份與光照修正）；缺素僅能以養分假說呈現；施肥建議僅在與本次症狀相關時出現，且不含施肥配方教學。
_Avoid_: AI 補足、罐頭施肥教學、僅一句含糊 summary

**養分假說**：
AI 分析中對缺素等營養問題的結構化推測（元素、可能性、視覺線索、無法單憑照片確診的但書）；不是確診。
_Avoid_: 缺素診斷、确诊缺铁
